{
  "name": "eon-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "postinstall": "run-s install:core",
    "install:core": "cd eon-core && npm i",
    "build": "run-s build:core",
    "build:core": "cd eon-core && npm run build",
    "test": "echo \"Future test will go here!\" && exit 0",
    "release": "run-s release:core",
    "release:core": "cd eon-core && npm run release"
  },
  "dependencies": {
    "npm-run-all": "4.1.5"
  },
  "license": "MIT"
}
