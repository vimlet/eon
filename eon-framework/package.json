{
  "name": "eon",
  "version": "0.9.11",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "@vimlet/meta": "2.0.8",
    "@vimlet/copy": "0.0.3",
    "@vimlet/less": "0.0.2",
    "npm-run-all": "4.1.3",
    "express": "4.16.4"
  },
  "scripts": {
    "build": "run-s meta copy less",
    "meta": "vimlet-meta -i src/meta/**/*.vmt -o build",
    "meta:watch": "vimlet-meta -i src/meta/**/*.vmt -o build -w build",
    "copy": "vimlet-copy -i src/copy/**/* -o build",
    "copy:watch": "vimlet-copy -i src/copy/**/* -o build -w build",
    "less": "vimlet-less -i src/less/**/* -o build",
    "less:watch": "vimlet-less -i src/less/**/* -o build -w build",
    "start": "node src/server/index.js"
  }
}
