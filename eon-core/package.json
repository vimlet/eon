{
  "name": "eon-core",
  "version": "0.9.11",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "@vimlet/metapack": "0.0.17",
    "@vimlet/commons-compress": "0.1.6",
    "express": "4.16.4"
  },
  "scripts": {
    "build": "npx metapack",
    "build:watch": "npx metapack --watch src",
    "release": "vimlet-compress -i 'build/eon/**/*' -o 'release/eon-core.zip'"
  }
}
